<% html_title t(:"ldap_group_sync.actions.new_mapping") %>

<%= render Primer::OpenProject::PageHeader.new do |header|
      header.with_title { t(:"ldap_group_sync.actions.new_mapping") }
    end %>

<%= render "form", url: { action: :create }, method: :post %>
