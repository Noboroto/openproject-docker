<% html_title t(:"auth_sso.edit_provider") %>
<%= render Primer::OpenProject::PageHeader.new do |header|
      header.with_title { "#{t(:'auth_sso.edit_provider')}: #{@provider.name}" }
      header.with_breadcrumbs(nil)
    end %>

<%= render "form",
           form_url: auth_sso_sso_provider_path(@provider),
           form_method: :patch %>
