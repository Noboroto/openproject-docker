<% html_title t(:"auth_sso.add_provider") %>
<%= render Primer::OpenProject::PageHeader.new do |header|
      header.with_title { t(:"auth_sso.add_provider") }
      header.with_breadcrumbs(nil)
    end %>

<%= render "form",
           form_url: auth_sso_sso_providers_path,
           form_method: :post %>
