<% html_title t(:"ldap_group_sync.actions.edit_mapping") %>

<%= render Primer::OpenProject::PageHeader.new do |header|
      header.with_title { t(:"ldap_group_sync.actions.edit_mapping") }
    end %>

<%= render "form", url: { action: :update, id: @mapping.id }, method: :patch %>
